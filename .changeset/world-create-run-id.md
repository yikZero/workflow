---
"@workflow/world": minor
---

Add optional `createRunId(input?)` to the `World` interface and `region` to `QueueOptions`. Worlds can now mint custom run IDs and route messages to a specific region.
