{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["WORKFLOW_PUBLIC_MANIFEST"],
      "outputs": [".output/**", ".nitro/**", ".tanstack/**"]
    }
  }
}
