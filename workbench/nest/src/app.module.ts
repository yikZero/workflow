import { createRequire } from 'node:module';
import { Module } from '@nestjs/common';
import { WorkflowModule } from 'workflow/nest';
import { AppController } from './app.controller.js';

// Force-enable manifest serving on Vercel
if (process.env.VERCEL) {
  process.env.WORKFLOW_PUBLIC_MANIFEST = '1';
}

// Load the manifest generated by workflow-nest build into globalThis.
// createRequire resolves ./workflow-manifest.json relative to THIS file
// (i.e. dist/workflow-manifest.json). The NestJS Vercel preset copies
// all of dist/ to the Lambda, so the file will be available.
const _require = createRequire(import.meta.url);
try {
  const _manifestObj = _require('./workflow/manifest.json');
  (globalThis as any).__workflowManifestJson = JSON.stringify(_manifestObj);
} catch {
  // File doesn't exist during dev (built at runtime by WorkflowModule)
}

@Module({
  imports: [
    WorkflowModule.forRoot({
      skipBuild: !!process.env.VERCEL,
    }),
  ],
  controllers: [AppController],
})
export class AppModule {}
